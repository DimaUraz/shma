import { Home, Notes, Tasks } from '@/pages'

export const ServicePages = {
  Home: {
    path: '/',
    component: Home
  },
  Notes: {
    path: '/notes',
    component: Notes
  },
  Tasks: {
    path: '/tasks',
    component: Tasks
  }
}
